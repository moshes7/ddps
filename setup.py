from setuptools import find_packages, setup

setup(name='ddps',
      version='0.1',
      description='',
      author = "Travis Team",
      author_email='',
      license='MIT',
      packages =find_packages(),
      zip_safe=False,
      )